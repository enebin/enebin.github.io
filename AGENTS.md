# 저장소 작업 규칙

- `/Users/kai.lee/.claude/claude.md`를 읽고 전역 규칙을 적용한다. 커밋·push·인증된 gh 명령 전에 저장소 경로별 신원과 계정 라우팅을 확인한다.
- 브랜치 이름에 `codex/` 접두사를 붙이지 않는다.
- 설명은 중학생도 이해할 수 있도록 쉽고 간결하게 쓴다.

## 글의 썸네일과 인트로

- 글 첫머리와 인트로에 썸네일·대표 이미지·커버 이미지를 넣지 않는다. 사용자가 해당 글에 명시적으로 요청한 경우만 예외다.
- front matter의 `image`와 기본 이미지(`/assets/og.png`)는 목록·SEO·링크 공유용이다. 이 값이 있다는 이유로 게시물 본문 위에 이미지를 자동 출력하지 않는다.
- 본문 내용을 설명하는 그림은 해당 섹션에 배치한다. 인트로 썸네일로 재사용하지 않는다.
- `write-article` 등 글 작성 스킬을 사용할 때도 이 규칙을 우선 적용하고, 미리보기에서 첫머리에 썸네일이 없는지 확인한다.
